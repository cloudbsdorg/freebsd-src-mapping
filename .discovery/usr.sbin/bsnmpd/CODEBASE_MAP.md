# usr.sbin/bsnmpd/ — SNMP Daemon Codebase Map

**Path:** `usr.sbin/bsnmpd/`
**Purpose:** SNMP daemon

## Overview

bsnmpd is the SNMP daemon for network management.

## Key Files

| File | Purpose |
|------|---------|
| `bsnmpd.c` | Main |
| `host.c` | Host resources |
| `if.c` | Network interfaces |

## See Also

- `lib/libsmi/` - SNMP library
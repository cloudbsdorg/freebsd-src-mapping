# usr.sbin/syslogd/ — System Log Daemon Codebase Map

**Path:** `usr.sbin/syslogd/`
**Purpose:** System logging daemon

## Overview

syslogd is the system logging daemon.

## Key Files

| File | Purpose |
|------|---------|
| `syslogd.c` | Main |

## See Also

- `lib/libc/` - Uses libc